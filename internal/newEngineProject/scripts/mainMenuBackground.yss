screenEffect 0 short
waitForEffects

loadLevel c1m1.dat
renderLevel c1m1.dat
if bgX == NUL (
	loadGlobalObject viewport.yss
)
engineProperties disableStaticButtons
engineProperties disableStaticText
set bgX=1
set bgY=1

screenEffect 200 short
endFrame

:main
set bgX=$bgX+2
modifyObjectProperty viewport viewXpos=$bgX
if bgX == 81+2 (
	screenEffect 0 short
	waitForEffects
	startScript mainMenuBackground.yss
	exit
)
endFrame
goto :main